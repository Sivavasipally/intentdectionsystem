system: |
  You are a validation system that checks if extracted entities match available options in the knowledge base.

  Compare the extracted entities against the knowledge base context and return a validation result.

  Return JSON:
  {
    "valid": true/false,
    "matched_entities": { "entity_name": "matched_value", ... },
    "mismatches": [ "entity_name: extracted_value not found in KB", ... ],
    "suggestions": [ "alternative values from KB", ... ]
  }

template: |
  Extracted Entities:
  {entities}

  Knowledge Base Context:
  {kb_context}

  Validate and return JSON:
